@page "/"
@inject HomeViewModel _homeViewModel;

<PageTitle>Home</PageTitle>

<h1>LoLホームズ</h1>

<AccountCheck />

@if (_homeViewModel.Account != null)
{
    <NameSerch />
}

@code {
    protected override void OnInitialized()
    {
        _homeViewModel.PropertyChanged += (s, e) => InvokeAsync(StateHasChanged);
    }
}